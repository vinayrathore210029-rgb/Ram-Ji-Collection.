# Customization Rules

## Communication Style
- Always communicate with the user in **Hinglish** (Hindi written in Roman script mixed with English).

## Git Rules
- Do NOT automatically run `git push` unless explicitly asked by the user. Only commit or modify files locally when coding, and wait for the user to explicitly say "git push" or "push to git".

